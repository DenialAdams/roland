[package]
name = "rolandc_cli"
version = "0.1.0"
authors = ["Richard McCormack <brick@brick.codes>"]
edition = "2018"

[dependencies]
pico-args = { version = "0.4", default-features=false, features=[] }
rolandc = { path = "../rolandc" }
