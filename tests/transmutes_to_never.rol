struct Foo {
   x: !
}

proc main() {
   () transmute Foo;
   () transmute !;
   () transmute [!; 1];
   0xCAFEBABE transmute &!;
}
__END__
compile:
Transmuting to the never type, a pointer to the never type, or a struct containing the never type isn't supported
↳ line 6, column 4 [tests/transmutes_to_never.rol]
Transmuting to the never type, a pointer to the never type, or a struct containing the never type isn't supported
↳ line 7, column 4 [tests/transmutes_to_never.rol]
Transmuting to the never type, a pointer to the never type, or a struct containing the never type isn't supported
↳ line 8, column 4 [tests/transmutes_to_never.rol]
Transmuting to the never type, a pointer to the never type, or a struct containing the never type isn't supported
↳ line 9, column 4 [tests/transmutes_to_never.rol]
