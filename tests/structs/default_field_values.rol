enum Color {
    Red,
    Blue,
}

struct Foo {
    a: u8 = 10,
    b: Color = Color::Red,
    c: u16 = 20,
    d: usize = "abc".length * 2,
}

proc main() {
    let x = Foo {a: 15};
    assert(x.a == 15);
    assert(x.b == Color::Red);
    assert(x.c == 20);
    println("OK");
}
__END__
run:
OK
