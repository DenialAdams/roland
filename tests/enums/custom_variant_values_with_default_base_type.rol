enum E {
    A = U64_MAX - 10
}

proc main() {
    println(uint_to_string(sizeof$<E>() as u64));
}
__END__
compile:
You must specify the base type of an enum if custom values are provided
↳ line 1, column 1 [tests/enums/custom_variant_values_with_default_base_type.rol]
