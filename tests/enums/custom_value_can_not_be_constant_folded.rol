enum Foo : u64 {
    A = great_proc(),
    B, // implicit value here should not error
    C = Foo::A transmute u64 + 3, // shouldn't get the a can't be constant folded message twice
}

proc great_proc() -> u64 {
    return 42;
}

proc main() {
}
__END__
compile:
Value of enum variant `Foo::A` can't be constant folded
↳ line 2, column 9 [tests/enums/custom_value_can_not_be_constant_folded.rol]
Value of enum variant `Foo::C` can't be constant folded
↳ line 4, column 9 [tests/enums/custom_value_can_not_be_constant_folded.rol]
