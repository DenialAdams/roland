proc main() {
   let x = unit();
   foo(x);
   foo(unit());
   println("hello");
}

proc foo(x: unit) {

}
__END__
compile:
Parameter `x` is unused
↳ line 8, column 10 [tests/procedures/unit_assignment_param.rol]
run:
hello
