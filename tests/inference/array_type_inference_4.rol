proc main() {
    let y = [1, 2, "x".pointer~, 4][0];
    let z: u8 = [1, 2, "x".pointer~, 4][0];
}
__END__
compile:
Local variable `y` is unused
↳ line 2, column 9 [tests/inference/array_type_inference_4.rol]
Local variable `z` is unused
↳ line 3, column 9 [tests/inference/array_type_inference_4.rol]
run:
