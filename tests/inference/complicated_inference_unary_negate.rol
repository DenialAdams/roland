proc main() {
   let x = 1;
   let y = -x;
   let z = [false, true][y];
   println_bool(z);
}
__END__
compile:
Unsigned integers (i.e. usize) can't be negated. Hint: Should this be a signed integer?
↳ line 3, column 12 [tests/inference/complicated_inference_unary_negate.rol]
