proc main() {
   let x = 1;
   let y = -x;
   let z = [false, true][y];
   println_bool(z);
}
__END__
compile:
Attempted to index an array with a value of type ?? Signed Int, which is not usize
↳ line 4, column 26 [tests/inference/complicated_inference_unary_negate.rol]
