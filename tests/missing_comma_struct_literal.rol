struct X {
   foo: i64,
   bar: u8
}

proc main() {
   let y = X {
      foo: 8
      bar: 10
   };
}
__END__
compile:
While parsing instantiation of struct `X`, encountered an unexpected identifier `bar`. Hint: Are you missing a comma?
↳ line 9, column 7 [missing_comma_struct_literal.rol]
