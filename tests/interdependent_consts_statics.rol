const X: usize = Y;
const Y: usize = Z.length + sizeof$u64();
const Z: [u64; 2] = [0, 0];

static A: usize = X;

proc main () {
   print_bool(X == 10);
   print_bool(Y == 10);
   print_bool(A == 10);
}
