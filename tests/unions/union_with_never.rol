union U {
   x: !
}

proc main() {
   let x: U = ___;
}
__END__
compile:
Variables of the never type, a pointer to the never type, or a struct or union containing the never type can't be uninitialized
↳ line 6, column 8 [tests/unions/union_with_never.rol]
