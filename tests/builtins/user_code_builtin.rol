builtin proc alacadabra();

proc main() {
   alacadabra();
   println("Everything is ok");
}
__END__
compile:
Procedure `alacadabra` is declared to be builtin, but only the compiler can declare builtin procedures
↳ line 1, column 1 [tests/builtins/user_code_builtin.rol]
