proc main() {
   a();
   b();
   println("finished");
}

proc a() -> () {
   let x: i16 = 5 + 5;
   return;
   println("never reached");
}

proc b() {
   let x: i16 = -10 + -10;
   return ();
   println("also never reached");
}
