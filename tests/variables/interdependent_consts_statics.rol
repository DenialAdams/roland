const X: usize = Y;
const Y: usize = Z.length + sizeof$<u64>();
const Z: [u64; 2] = [0, 0];

static A: usize = X;

proc main () {
   println_bool(X == 10);
   println_bool(Y == 10);
   println_bool(A == 10);
}
__END__
run:
true
true
true
