static X: u64 = Y;
static Y: u64 = X;

proc main() {}
__END__
compile:
Value of static `X` can't be constant folded. Hint: Either simplify the expression, or initialize it yourself on program start.
↳ line 1, column 17 [tests/variables/statics/recursive_static.rol]
Value of static `Y` can't be constant folded. Hint: Either simplify the expression, or initialize it yourself on program start.
↳ line 2, column 17 [tests/variables/statics/recursive_static.rol]
