proc main() {
   oh_no();
}

proc oh_no() -> u8 {
   let x: ! = ___;
   x;
}__END__
compile:
Variables of the never type, a pointer to the never type, or a struct containing the never type can't be uninitialized
↳ line 6, column 8 [tests/never/uninit_never.rol]
