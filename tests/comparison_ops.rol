proc main() {
   let x: i64 = -10;
   let y: i64 = 10;

   println_bool(x > y);
   println_bool(x >= y);
   println_bool(x < y);
   println_bool(x <= y);
   println_bool(x == y);
   println_bool(x != y);

   let z: i64 = 10;

   println_bool(y > z);
   println_bool(y >= z);
   println_bool(y < z);
   println_bool(y <= z);
   println_bool(y == z);
   println_bool(y != z);
}
